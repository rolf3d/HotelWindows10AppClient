<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotelWindoes10App.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:HotelWindoes10App.ViewModel"
    x:Class="HotelWindoes10App.View.GuestPage"
    mc:Ignorable="d">
    <Page.TopAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Accept" Label="appbarbutton"/>
            <AppBarButton Icon="Cancel" Label="appbarbutton"/>
        </CommandBar>
    </Page.TopAppBar>

    <Page.DataContext>
        <ViewModel:GuestViewModel/>
    </Page.DataContext>

    <Viewbox>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Height="900" Width="1300">
        <ListView HorizontalAlignment="Left" Height="675" Margin="83,90,0,0" VerticalAlignment="Top" Width="554" ItemsSource="{Binding GuestCatalogSingleton.Guests}"/>
        <Button Content="Ny gæst" HorizontalAlignment="Left" Margin="83,810,0,0" VerticalAlignment="Top" Height="52" Width="293"/>


    </Grid>
        </Viewbox>
</Page>
